name: ci
on:
  push:
    branches:
      # "*" stops at a slash, so it misses every feature branch.
      - "**"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    name: test
    env:
      RUST_BACKTRACE: 1
      GVSBUILD_VERSION: 2026.8.0
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-24.04
            rust: stable
          # - os: ubuntu-24.04
          #   rust: nightly
          - os: windows-latest
            rust: stable
          # - os: windows-latest
          #   rust: nightly

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Install packages (Ubuntu)
      if: matrix.os == 'ubuntu-24.04'
      run: |
        ci/ubuntu-install-packages

    # pkg-config.exe ships inside the gvsbuild zip, so only libclang is missing.
    - name: Install LLVM (Windows)
      if: matrix.os == 'windows-latest'
      run: choco install -y --no-progress llvm

    - name: Cache GTK4 runtime (Windows)
      if: matrix.os == 'windows-latest'
      id: gtk-cache
      uses: actions/cache@v4
      with:
        path: C:\gtk
        key: gvsbuild-gtk4-${{ env.GVSBUILD_VERSION }}

    - name: Download GTK4 runtime (Windows)
      if: matrix.os == 'windows-latest' && steps.gtk-cache.outputs.cache-hit != 'true'
      run: |
        $url = "https://github.com/wingtk/gvsbuild/releases/download/$env:GVSBUILD_VERSION/GTK4_Gvsbuild_${env:GVSBUILD_VERSION}_x64.zip"
        Invoke-WebRequest -Uri $url -OutFile gtk4.zip
        7z x gtk4.zip -oC:\gtk

    # bindgen runs libclang over MuPDF headers, so it needs the CRT and SDK include paths.
    - name: Set MSVC environment (Windows)
      if: matrix.os == 'windows-latest'
      shell: cmd
      run: |
        rem -products is required: vswhere skips Build Tools installs without it.
        pushd "%ProgramFiles(x86)%\Microsoft Visual Studio\Installer"
        for /f "usebackq tokens=*" %%i in (`vswhere.exe -latest -products "*" -property installationPath`) do set "VSPATH=%%i"
        popd
        call "%VSPATH%\VC\Auxiliary\Build\vcvars64.bat"
        echo INCLUDE=%INCLUDE%>> %GITHUB_ENV%
        echo LIB=%LIB%>> %GITHUB_ENV%
        echo LIBPATH=%LIBPATH%>> %GITHUB_ENV%

    - name: Set GTK environment (Windows)
      if: matrix.os == 'windows-latest'
      run: |
        "C:\gtk\bin" | Out-File -FilePath $env:GITHUB_PATH -Append -Encoding utf8
        @(
          "PKG_CONFIG_PATH=C:\gtk\lib\pkgconfig"
          "LIBCLANG_PATH=C:\Program Files\LLVM\bin"
        ) | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8

    - name: Install Rust
      uses: dtolnay/rust-toolchain@master
      with:
        toolchain: ${{ matrix.rust }}
        components: rustfmt, clippy

    # Formatting is platform independent, and the Windows runner checks out CRLF.
    - name: Check formatting
      if: matrix.os == 'ubuntu-24.04'
      run: cargo fmt --all -- --check

    - name: Run Clippy
      # No --all-features: it would enable `bench` and pull criterion back in.
      run: cargo clippy --all-targets -- -D warnings

    - name: Check Flatpak cargo sources are in sync with Cargo.lock
      if: matrix.os == 'ubuntu-24.04'
      run: ci/check-cargo-sources

    - name: Build
      run: cargo build --verbose

    - name: Run tests (Ubuntu)
      if: matrix.os == 'ubuntu-24.04'
      run: xvfb-run --auto-servernum cargo test --verbose

    - name: Run tests (Windows)
      if: matrix.os == 'windows-latest'
      run: cargo test --verbose

  audit:
    name: audit
    runs-on: ubuntu-24.04
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Install cargo-audit
      uses: taiki-e/install-action@v2
      with:
        tool: cargo-audit

    # Reads Cargo.lock only. No GTK or MuPDF toolchain needed.
    - name: Audit dependencies
      run: cargo audit
